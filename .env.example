# Copy to .env before running docker compose. Every value has a working default
# except JWT_SECRET, which you must change before exposing the platform.

POSTGRES_PASSWORD=twip
JWT_SECRET=generate-with-openssl-rand-hex-32
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
