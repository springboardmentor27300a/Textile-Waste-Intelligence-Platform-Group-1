version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build step required for plain HTML/JS/CSS"
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
